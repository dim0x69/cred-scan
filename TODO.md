1. add --extract with max size
2. add 512 KB scan limit (global)
3. Whenever the tasks below change persisted workspace formats, migrate the user's one existing workspace as part of that task. Preserve accumulated results and evidence; use a migration tailored to the actual workspace rather than a general compatibility framework.
5. Keep only the latest selected scan targets in inventory; stop retaining superseded scan targets once historical occurrence reads are independent of inventory. Preserve historical credentials, occurrence paths, judgments, and evidence. Update inventory merging, content-reader contracts, schemas, tests, and the visual guide together. Use “scan target” consistently rather than “pin” as a name for a target.
6. Allow evidence extraction for both VALID and UNKNOWN judgments. Update extraction eligibility, extraction processing, tests, and documentation; current code permits only VALID. Preserve the saved judgment if extraction fails.
7. Instruct the judge to prefer reading the first occurrence's file, while allowing additional occurrence reads. Cache retrieved files for reuse by extraction. Extraction must still retain only the first occurrence's file, using its cached bytes when available; additional cached files do not become retained evidence. Ensure the sequential boundary workflow shares the reader/cache between judgment and extraction, and update tests and documentation.
8. Check only file existence for already-retained evidence on later runs. Report missing files visibly without automatic replacement; do not read contents or verify size/hash. Current scheduling skips RETAINED records entirely, so ensure the existence check runs even when no extraction is pending. Update tests and documentation.
9. Scope retrieved-file caching to one boundary workflow, in memory only. Share cached reads across scan, judgment, and extraction, then clear them when the workflow finishes, including failure/cancellation cleanup. Do not persist a retrieved-file cache to disk or reuse it across workflows or runs. Keep retained evidence separate from this temporary cache; update lifecycle tests and documentation.
10. Align configuration sources with the agreed separation: runtime settings from config.yml; API keys and other secrets from environment variables or .env. Current settings sources accept YAML secrets and environment-provided nonsecret settings; restrict those sources and update configuration tests and documentation.
11. Preserve the previous inventory on failed or partial discovery, recording the error without replacing scan targets. Cover this behavior when implementing latest-only inventory, including discovery that returns some results alongside errors.
12. make extract a separate module, move models and functions there.
13. Replace the workspace’s default Artifactory `.backend` marker with `backend.json`, containing only the backend name (for example, `"artifactory_docker"`). Make that name the discriminator used when loading a workspace and selecting the concrete `BackendAdapter`. Remove the current `BackendConfig` role: backend connection and adapter-specific properties belong on the adapter implementation or an appropriate adapter superclass, while concrete adapters such as `ArtifactoryDockerBackend` may add their own properties. Treat the current `kind` value as the backend name, and serialize only that name in `backend.json`; do not duplicate adapter configuration there. Construct the selected backend once while loading the workspace, pass the backend object into each boundary, and use that object throughout boundary operations. Align all backends to the same `BackendAdapter` → concrete implementation structure, allowing shared superclasses where useful (such as the Artifactory transport layer). Migrate the existing workspace marker as part of implementation and update loading, persistence, tests, and documentation together.
